{% extends 'base_admin.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <h1>Modification de Compte</h1>

    <section class="container mt-2 mb-2">
        <div class="row">
            <div class="col-md-6 offset-3 mt-2">
                <fieldset class="mb-3">
                    {{ form_start(form) }}
                        {{ form_row(form.solde) }}
                         {{ include('admin/comptes/_form.html.twig', {'button_label': 'Modifier'}) }}
                        <a href="{{ path('app_comptes_index') }}"class="btn btn-lg btn-cyan mt-2">retour</a>
                        {{ include('admin/comptes/_delete_form.html.twig') }}
                    {{ form_end(form) }}
                </fieldset>
            </div>
        </div>
    </section>
{% endblock %}
