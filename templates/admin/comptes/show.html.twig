{% extends 'base_admin.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <h1>Compte N°{{compte.id}} </h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compte.id }}</td>
            </tr>
            <tr>
                <th>Solde</th>
                <td>{{ compte.solde }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_comptes_index') }}">retour</a>

    <a href="{{ path('app_comptes_edit', {'id': compte.id}) }}">Modifier</a>

    {{ include('admin/comptes/_delete_form.html.twig') }}
{% endblock %}
