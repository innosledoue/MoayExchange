{% extends 'base_admin.html.twig' %}

{% block title %}Commandes{% endblock %}

{% block body %}
    <h1>Commandes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>MontantEnvoye</th>
                <td>{{ commande.montantEnvoye }}</td>
            </tr>
            <tr>
                <th>MontantRecevoir</th>
                <td>{{ commande.montantRecevoir }}</td>
            </tr>
            <tr>
                <th>AdresseReception</th>
                <td>{{ commande.adresseReception }}</td>
            </tr>
            <tr>
                <th>AdresseEnvoyeur</th>
                <td>{{ commande.adresseEnvoyeur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commandes_index') }}">back to list</a>

    <a href="{{ path('app_commandes_edit', {'id': commande.id}) }}">edit</a>

    {{ include('admin/commandes/_delete_form.html.twig') }}
{% endblock %}
