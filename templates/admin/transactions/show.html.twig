{% extends 'base_admin.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <h2>Transaction N°{{transaction.id}}</h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transaction.id }}</td>
            </tr>
            <tr>
                <th>Type_transaction</th>
                <td>{{ transaction.typeTransaction }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ transaction.montant }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ transaction.createdAt ? transaction.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_transactions_index') }}">retour</a>

    <a href="{{ path('app_transactions_edit', {'id': transaction.id}) }}">Modifier</a>

    {{ include('transactions/_delete_form.html.twig') }}
{% endblock %}
