<form method="post" action="{{ path('app_crypto_monnaie_delete', {'id': crypto_monnaie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ crypto_monnaie.id) }}">
    <button class="btn">Delete</button>
</form>
