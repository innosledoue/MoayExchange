{% extends 'base_admin.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <h2>CryptoMonnaie N°{{crypto_monnaie.id}} </h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ crypto_monnaie.id }}</td>
            </tr>
            <tr>
                <th>Crypto_name</th>
                <td>{{ crypto_monnaie.cryptoName }}</td>
            </tr>
            <tr>
                <th>Symbole</th>
                <td>{{ crypto_monnaie.symbole }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_crypto_monnaie_index') }}">retour</a>

    <a href="{{ path('app_crypto_monnaie_edit', {'id': crypto_monnaie.id}) }}">Modifier</a>

    {{ include('crypto_monnaie/_delete_form.html.twig') }}
{% endblock %}
