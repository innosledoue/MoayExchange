{% extends 'base_admin.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <h2>Création de CryptoMonnaie</h2>

    <section class="container mt-2 mb-2">
        <div class="row">
            <div class="col-md-6 offset-3 mt-2">
                <fieldset class="mb-3">
                    {{ form_start(form) }}
                        {{ form_row(form.crypto_name) }}
                        {{ form_row(form.symbole) }}
                        <button class="btn btn-lg btn-success mt-2">{{ button_label|default('Enregistrer') }}</button>
                         <a href="{{ path('app_crypto_monnaie_index') }}" class="btn btn-lg btn-cyan mt-2"> retour</a>
                    {{ form_end(form) }}
                </fieldset>
            </div>
        </div>
    </section>
{% endblock %}
